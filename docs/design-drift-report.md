# Design Drift Report

Generated by design-sync Step 4. Ground truth: `design/preview.html` + `design/tokens.md`.

---

## Summary

| Component | File | Token Drift | Structural Drift |
|-----------|------|-------------|-----------------|
| Button | src/components/ui/button.tsx | Yes | Yes |
| Input | src/components/ui/input.tsx | Yes | No |
| StatusBadge | src/components/ui/badge.tsx | No | No |
| Card | src/components/ui/card.tsx | Yes | Yes |
| Dialog/Modal | src/components/ui/dialog.tsx | Yes | Yes |
| Tabs | src/components/ui/tabs.tsx | No | Yes |
| Skeleton | src/components/ui/skeleton.tsx | Yes | Yes |
| AppShell | src/components/features/shell/AppShell.tsx | No | Yes |

---

## Button — `src/components/ui/button.tsx`

### Token drift (auto-fixable)
| Location | Current | Canonical |
|----------|---------|-----------|
| Default height | `h-8` (32px) | `min-height: 36px` |
| Large height | `h-9` (36px for lg) | `min-height: 44px` |

### Structural drift (requires decision)
| # | Finding | Canonical | Options |
|---|---------|-----------|---------|
| B-S1 | Variant name `default` | Should be `primary` | Fix / Keep |
| B-S2 | Variant `outline` exists | Not in preview — preview has primary/secondary/ghost/destructive only | Fix (remove) / Keep |
| B-S3 | `secondary` variant uses `bg-bg-tertiary` | Should be `bg-surface` (`var(--color-background-primary)` with border) | Fix / Keep |
| B-S4 | Loading state not implemented | Canonical has spinner + disabled state during load | Fix / Defer |

---

## Input — `src/components/ui/input.tsx`

### Token drift (auto-fixable)
| Location | Current | Canonical |
|----------|---------|-----------|
| Default border | `border-border` (`--color-border`) | `border-border-strong` (`--color-border-strong`) |
| Height | `h-8` (32px) | `min-height: 36px` |

### Structural drift
None beyond token drift.

---

## StatusBadge — `src/components/ui/badge.tsx`

No drift. Fully matches canonical implementation.

---

## Card — `src/components/ui/card.tsx`

### Token drift (auto-fixable)
| Location | Current | Canonical |
|----------|---------|-----------|
| Card background | `bg-card` (shadcn token — not in `design/tokens.md`) | `var(--color-background-primary)` |
| Card text | `text-card-foreground` (shadcn token — not in `design/tokens.md`) | `var(--color-text-primary)` |
| Muted areas | `bg-muted/50` (shadcn token — not in `design/tokens.md`) | `var(--color-background-secondary)` |

### Structural drift (requires decision)
| # | Finding | Canonical | Options |
|---|---------|-----------|---------|
| C-S1 | Border: `ring-1 ring-foreground/10` | Should use `border border-border` | Fix / Keep |
| C-S2 | CardHeader/Footer missing `background-secondary` + bottom/top border separator | Canonical: header has `bg-[--color-background-secondary]` + `border-b`; footer same | Fix / Keep |
| C-S3 | Extra subcomponents `CardAction`, `CardDescription` | Not in preview | Keep (non-breaking additive) / Remove |

---

## Dialog — `src/components/ui/dialog.tsx` (maps to canonical Modal)

### Token drift (auto-fixable)
| Location | Current | Canonical |
|----------|---------|-----------|
| Backdrop color | `bg-black/20` | `background: rgb(15 23 42 / 0.3)` |

### Structural drift (requires decision)
| # | Finding | Canonical | Options |
|---|---------|-----------|---------|
| D-S1 | Trigger/Portal pattern (uncontrolled) | Canonical Modal is controlled (`open` + `onClose` props) | Fix / Keep |
| D-S2 | No `maxWidth` prop | Canonical has `maxWidth?: string` | Fix / Defer |
| D-S3 | Close on backdrop click not wired | Canonical closes on backdrop click | Fix / Keep |

---

## Tabs — `src/components/ui/tabs.tsx`

### Token drift
None.

### Structural drift (requires decision)
| # | Finding | Canonical | Options |
|---|---------|-----------|---------|
| T-S1 | Has two variants: default (pills) + `line` | Preview shows only pill/button style tabs | Keep (line variant is additive) / Fix (remove line variant) |
| T-S2 | Active indicator via `after:` pseudo-element + translate | Canonical uses `aria-selected` with direct style | Keep (functionally equivalent) / Fix |

---

## Skeleton — `src/components/ui/skeleton.tsx`

### Token drift (auto-fixable)
| Location | Current | Canonical |
|----------|---------|-----------|
| Background | `bg-muted` (shadcn token — not in `design/tokens.md`) | `var(--color-background-tertiary)` |
| Animation | `animate-pulse` (opacity fade) | Shimmer gradient (`--shimmer-start` → `--shimmer-end`) |

### Structural drift (requires decision)
| # | Finding | Canonical | Options |
|---|---------|-----------|---------|
| SK-S1 | No `Spinner` component | Canonical `Skeleton.tsx` exports `Spinner` (border-based animated ring) | Fix / Defer |
| SK-S2 | No `SkeletonText`, `SkeletonCard` variants | Canonical exports these compound patterns | Fix / Defer |

---

## AppShell — `src/components/features/shell/AppShell.tsx`

### Token drift
None — uses Tailwind class names that map to tokens correctly.

### Structural drift (requires decision)
| # | Finding | Canonical | Options |
|---|---------|-----------|---------|
| A-S1 | Sidebar width `w-64` (256px) | Canonical: 220px | Fix / Keep |
| A-S2 | Topbar height `h-16` (64px) | Canonical: 52px (`3.25rem`) | Fix / Keep |
| A-S3 | No brand mark ("Tetra" wordmark in sidebar) | Canonical has brand mark at sidebar top | Fix / Defer |
| A-S4 | No role badge under user name | Canonical shows role label (admin/company/customer) | Fix / Defer |
| A-S5 | No breadcrumb in topbar | Canonical renders breadcrumb trail from `breadcrumb` prop | Fix / Defer |

---

## Decisions needed

For each structural drift item, mark: **Fix** (align to canonical), **Keep** (keep current behavior), or **Defer** (log and skip).

Auto-fixable token drift items will be applied automatically once you confirm to proceed to step 5.
